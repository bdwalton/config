#!/bin/bash

# A list of debian packages we expect in our environment.
PACKAGES=(
    autorandr
    dex
    gconf2
    gnome-screensaver
    i3
    inputplug
    keychain
    pulseaudio-utils
    rofi
    thunar
    tmux
    xautolock
)

sudo apt install ${PACKAGES[@]}
