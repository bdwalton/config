# -*- shell-script -*-

# on any of the cswbo build boxes, we need to setup the proxy
case "${HOSTNAME}" in
    pandora) ;; # this box has direct net access
    *) export http_proxy=http://192.168.1.6:3128;;
esac
