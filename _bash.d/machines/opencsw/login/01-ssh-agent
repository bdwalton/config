# setup ssh-agent on the buildfarm
if [ -f ~/.ssh-agent ]; then
    source ~/.ssh-agent
fi

if [ -z "$SSH_AUTH_SOCK" -o ! -w "$SSH_AUTH_SOCK" ]; then
    ssh-agent -s >~/.ssh-agent && \
        source ~/.ssh-agent && \
        ssh-add
fi
