# -*- shell-script -*-
# prefer emacs, use vim, then vi

for ed in /opt/csw/bin/emacs /opt/csw/bin/vim /usr/bin/vi; do
    if [ -f $ed ]; then
       export EDITOR=$ed
       break
    fi
done

export VISUAL=$EDITOR
