# -*- shell-script -*-

# if we have gnu versions of these utilities, we'll enable a nicer
# pair of aliases for lc/lca.
if [ -f /opt/csw/bin/gls ]; then
    GNULS=true
else
    GNULS=
fi

if [ -f /opt/csw/bin/gegrep ]; then
    GNUGREP=true
else
    GNUGREP=
fi
