# -*- shell-script -*-
# if su hasn't set $HOME appropriately on a Sun box, fix that and bail

if [ $UID -eq 0 ]; then
    #sol8 vs sol10 home dir differences (we set home with cfengine in sol10)   
    if [ -d /root ]; then
	export HOME=/root
    else
	export HOME=/
    fi
fi