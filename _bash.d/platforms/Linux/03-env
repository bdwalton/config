# fallback to a new emacs session if we don't have a client running somewhere
EDITOR=emacsclient
ALTERNATE_EDITOR=emacs

# Make less use 2 as the tabstop
LESS="-x2 -E -X -R -M"

export EDITOR ALTERNATE_EDITOR LESS
